# Python FastAPI Project Setup

## 1. Create project
- Create repo (no template)
- Create backend/app structure

## 2. Virtual environment
```bash
cd backend
python -m venv .venv
.venv\Scripts\Activate.ps1   # Windows
source .venv/bin/activate   # Mac/Linux
